//! pack-source/ 를 입력받아 .mrpack + manifests/<ver>.json 을 생성한다.
//!
//! Phase 2에서 실제 구현. 지금은 엔트리 포인트만.

fn main() {
    eprintln!("cherishpack-manifest-builder — Phase 2에서 구현 예정");
    eprintln!("사용법 (예정): cherishpack-manifest-builder <pack-source> <out-dir> <version>");
    std::process::exit(2);
}
